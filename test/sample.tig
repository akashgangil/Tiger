type ArrayInt = array [101] of int;
void main()
begin
  begin
    var X, Y : ArrayInt := 10;
    var i, sum : int := 0;
    begin
      for i := 0 to 100 do
        sum := sum + X[i] * Y[i];
      enddo;
      printi(sum);
    end;
  end;
end;
