int function addNums2(a:int, b:int)
begin
    begin
        return a + b;
    end;
end;

int function addNums(a:int, b:int)
begin
    begin
    	return addNums2(a, b);
    end;
end;

int function multNums(a:int, b:int)
begin
    begin
        var total:int := 0;
        var i:int := 0;
        while i < b do
            total := total + a;
            i := i + 1;
        enddo;
        return total;
    end;
end;

void function myPrint(a:int)
begin
    begin
        printi(a);
    end;
end;

void main()
begin
    begin
        var s:int := 100;
        var a:int := 3;
        var product:int := 0;
        product := multNums(s, a);
        myPrint(product);
    end;
end;
